---
- hosts: all
  tasks:
    - name: hour
      shell: echo "{{ schedules }}" > schedules && cat schedules
      register: hour
      
    - name: days
      shell: echo "{{ days }}" > days && cat days
      register: days

    - name: hour
      debug:
        var: hour.stdout
    - name: days
      debug:
        var: days.stdout
